#include<stdio.h>//#=preprocessor directive   stdio.h=standard input output
void main(){//void=datatype main=function
    printf("Madhav");
    getch();//Holds output screen
}
#include<stdio.h>//stdio.h updates the values every time we run the code.
#include<conio.h>//conio.h=console inpit output, it does not store any values and assignes new boxes every time we run the code.
int main(){
    int a;//a=variable
    a=4;//= is assignment operator
    printf("Input two integer values = ");
    printf("%d", a);//%d-specifies int
    //%d, %c, %f, %s, etc.  format specifier
}
//pure code mein main() sirf ek hi ho sakta hai.