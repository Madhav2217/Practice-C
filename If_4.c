#include<stdio.h>
void main(){
#include<stdio.h>
void main(){
    int a = 500, b = 100, c;
    if (!a>=400)            // without {} the first statement after if is considered in if and all others are not.
        b=300;
        c=200;
        printf("%d %d", b, c);



    /*
    int a = 10, b = 10;

    if (a=5)
    
        b--;
        printf("%d %d", a, b--);
*/
}